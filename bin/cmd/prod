#!/usr/bin/env bash

# change to the project root
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$DIR"/../.. || exit

# setup dependencies
bin/setup
# build application
echo "Building..."
go build -o production_build
# deprecated_run build
echo "Running..."
./production_build