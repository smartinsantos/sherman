#!/usr/bin/env bash

# change to the project root
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$DIR"/.. || exit

echo "Downloading dependencies..."
bin/setup

echo "Building..."
go build -o production_build

echo "Running..."
./production_build